# 컨텐츠 마스터즈 — 자주 쓰는 명령어

## 앱 켜기 / 끄기
```bash
open ~/Desktop/"▶ 컨텐츠 마스터즈.app"
```
```bash
lsof -ti tcp:5173 | xargs kill
```

## 깃허브에 올리기 (배포)
바탕화면 폴더 = 저장소 `isuseong454-hub/Content-Masters` (2026-08-20 연결 완료)
사이트: https://isuseong454-hub.github.io/Content-Masters/

```bash
cd ~/Desktop/monetize-content-masters && git status --short
```
```bash
cd ~/Desktop/monetize-content-masters && git add -A && git commit -m "v___: 무엇을 고쳤는지" && git push origin main
```
> ⚠️ 올리기 전 `sw.js`의 `CACHE_NAME` 버전을 올릴 것. 안 올리면 기존 사용자에게 새 버전이 안 내려간다.

## 배포 상태 확인
```bash
cd ~/Desktop/monetize-content-masters && ~/.local/bin/gh api repos/isuseong454-hub/Content-Masters/pages/builds/latest --jq '{status:.status,created:.created_at}'
```

## 깃허브 로그인 확인
```bash
~/.local/bin/gh auth status
```

## 인스타 API — 내 계정 확인
```bash
TK=$(tr -d '\n\r ' < ~/Desktop/meta-token.txt); curl -s "https://graph.instagram.com/v23.0/me?fields=username,account_type,followers_count,media_count&access_token=$TK"
```

## 인스타 API — 내 릴스 인사이트 (조회수·저장·공유)
```bash
TK=$(tr -d '\n\r ' < ~/Desktop/meta-token.txt); curl -s "https://graph.instagram.com/v23.0/me/media?fields=id,permalink,media_product_type,timestamp&limit=5&access_token=$TK"
```
```bash
TK=$(tr -d '\n\r ' < ~/Desktop/meta-token.txt); MID=미디어아이디; curl -s "https://graph.instagram.com/v23.0/$MID/insights?metric=views,reach,saved,shares,likes,comments&access_token=$TK"
```

## 백업 (고치기 전에)
```bash
cd ~/Desktop/monetize-content-masters && cp index.html "index.bak-$(date +%Y%m%d-%H%M).html"
```

---

## 메모
- 메타 앱 ID `1800246251145754` · 클라이언트 토큰은 `~/Desktop/meta-app.txt`
- **oEmbed(남의 릴스 썸네일)는 검수 통과 전엔 안 됨** — 2026-08-21 실제 확인
- 저장함·컬렉션은 API에 아예 없음
- 백업본·`supabase/`·`출판키 메모장`은 `.gitignore`로 막혀 있어 안 올라감

---

# 🔐 유튜브 열쇠 금고 (2026-08-21 · v805)

**끝난 것 (클로드)**
- SQL `supabase/step14-yt-vault.sql` 실행 완료 — `cm_yt_ledger` · `cm_yt_cache` · `cm_yt_gate` · `cm_yt_done` · `cm_yt_today`
- 엣지 함수 `yt` 배포 완료 (ACTIVE) — 소스: `supabase/functions/yt/index.ts`
- `index.html` 에서 평문 열쇠 삭제 · 유튜브 직접 호출 7군데 → 창구(`sctYt`)로 교체
- 정찰 화면에 「오늘 검색 N/10」 표시 · 한도 차도 **담기는 계속 됨**
- `sw.js` 캐시 v805
- 점검표(`_smoke.js`)에 열쇠 3항목 추가 — 평문 열쇠가 되살아나면 배포 전에 걸린다

## ✅ 2026-08-21 완료 — 실제로 돌아갑니다
- `YT_KEY` 슈파베이스에 꽂힘 (구글 프로젝트 `contentmasters`, 「수성」 계정)
- 검색 정상 · 캐시 정상(2563ms→169ms, 점수 0) · **11번째에 막힘** · 막혀도 담기는 됨
- **관리자 무제한**: `Z7PBCA / 새로이` 에 `is_master = true` 켬 (2026-08-21)

## ★아직 남은 숙제 2개

~~**① 컨모 열쇠 분리**~~ → ✅ 2026-08-21 완료.
   구글 프로젝트 `contentmodeun`(계정 `isuseong454@gmail.com`) 새 열쇠 발급 →
   `~/.컨모열쇠.json` 교체 → 유튜브 호출 확인됨.

**① 🚨 옛 열쇠 폐기 (이제 이것만 남았다)** — `AIzaSyAx6v…` · 구글 프로젝트 번호 `23338785296` · 계정 `qkdna12`
   ⚠️ 계정 `isuseong454@gmail.com`(=「수성」)에는 **이 프로젝트 권한이 없다.** 확인됨.
   비번 복구 48시간 대기 중(2026-08-21 기준). 복구되면 바로:
   `https://console.cloud.google.com/apis/credentials?project=23338785296` → 🗑 삭제
   ⚠️ **이 열쇠는 지금도 살아 있고 깃허브 기록에 남아 있다. 지워야만 끝난다.**
   ✅ 이제 컨마·컨모 둘 다 새 열쇠로 갈아탔으므로 **지워도 아무것도 안 멈춘다.**

## (참고) 옛 지시서의 사장님 단계

`console.cloud.google.com` → **API 및 서비스 → 사용자 인증 정보**

1. **새 열쇠 발급** — 제한은 **「없음」 또는 IP 제한**
   (엣지 함수에서만 쓰므로 주소 제한이 필요 없다)
2. **슈파베이스에 꽂기**
   `Supabase 대시보드 → Edge Functions → Secrets → YT_KEY = 새열쇠`
3. **옛 열쇠 폐기** — `AIzaSyAx6v…` 로 시작하던 것.
   ⚠️ 이미 공개됐으므로 **숨기는 걸로는 안 되고 반드시 삭제**해야 한다
4. 같은 화면 **사용량 그래프** 확인 — 안 쓴 날에도 숫자가 오르면 남이 쓰는 것

## 열쇠 꽂은 뒤 확인할 것

- [ ] 수강생 계정으로 정찰 검색이 된다
- [ ] 11번째 검색에서 막히고, **링크로 담기는 여전히 된다**
- [ ] 사장님(관리자) 계정은 몇 번을 해도 안 막힌다
- [ ] 같은 검색어를 다시 치면 즉시 나온다(캐시 · 점수 0)
- [ ] 옛 열쇠를 지운 뒤에도 앱이 정상

## 창구 직접 찔러보기
```bash
curl -s -X POST "https://azmayrzdzdeftrjzcycf.supabase.co/functions/v1/yt" -H "Content-Type: application/json" -d '{"kind":"search","params":{"q":"테스트"},"token":"없는토큰"}'
```
→ `{"ok":false,"reason":"nosession"}` 이 나오면 창구는 정상.

## 오늘 쓴 양 보기 (SQL)
```sql
select * from cm_yt_ledger where day = current_date order by units desc;
```
